/** Unified quote request inbox. The existing history view already contains
 * every decision state, including escalated requests; this route gives it a
 * clearer product name while preserving the old URL for compatibility. */
export { default, loader, action } from "./app.pricefloor.history";
